# 麻的小辛辣 員工打卡系統（階段一：純記錄）

設計文件：`/Users/guoeason/mala-schedule/docs/superpowers/specs/2026-07-05-employee-clock-in-design.md`

## 檔案

- `clock.html` — 員工打卡頁（手機開啟，網址帶 `?k=員工專屬key`）
- `manager.html` — 值班主管核定頁（手機開啟，網址帶 `?k=主管專屬key`，見下方「值班主管核定」一節）
- `apps-script/Code.gs` — Google Apps Script 後端（貼到 Apps Script 編輯器）
- `mock/mock_server.py` — 本機模擬後端（Python3 標準庫，免裝套件）
- `mock/mock_data.json` — 模擬後端的資料檔（首次執行自動產生，含種子員工／種子主管）

## 本機測試步驟

1. 啟動模擬後端：
   ```
   cd /Users/guoeason/mala-clock-in/mock
   python3 mock_server.py
   ```
   預設監聽 `http://localhost:8899`，同時服務 repo 靜態檔＋`/api` 端點。

2. 用手機瀏覽器（或電腦瀏覽器）打開：
   ```
   http://localhost:8899/clock.html?k=testkey1&api=/api
   ```
   - `k=testkey1` → 測試員工「測試一」（E01）
   - `k=testkey2` → 測試員工「測試二」（E02）
   - `?api=/api` 只在 `localhost`/`127.0.0.1` 生效，用來覆蓋 clock.html 內建的正式 API_URL 佔位字串；正式環境不需要也不能加這個參數。

3. 手機打開時瀏覽器會跳出「允許定位」，需允許才能打卡。若在電腦上測試，Chrome 開發者工具可用 Sensors 面板模擬座標。

3-b. 值班主管核定頁（不需要定位）：
   ```
   http://localhost:8899/manager.html?k=testmgr1&api=/api
   ```
   `k=testmgr1` → 種子主管「測試主管」。

4. 管理端點（admin_key 固定 `test-admin`，僅本機測試用）：
   ```bash
   # 查詢名冊
   curl -s -X POST http://localhost:8899/api \
     -d '{"action":"get_roster","admin_key":"test-admin"}'

   # 查詢近 31 天打卡紀錄
   curl -s -X POST http://localhost:8899/api \
     -d '{"action":"get_events","admin_key":"test-admin"}'

   # 核准/拒絕新裝置
   curl -s -X POST http://localhost:8899/api \
     -d '{"action":"approve_device","admin_key":"test-admin","emp_id":"E01","device_id":"新裝置碼","approve":true}'

   # 名冊同步（新增/更新員工，不覆蓋既有 key/device_id）
   curl -s -X POST http://localhost:8899/api \
     -d '{"action":"sync_roster","admin_key":"test-admin","employees":[{"emp_id":"E03","name":"測試三","active":true}]}'
   ```

5. 重置測試資料：關掉 mock server 後刪除 `mock/mock_data.json`，下次啟動會重新產生種子資料（E01/testkey1、E02/testkey2）。

## 日後部署步驟（正式上線前，動手前務必先跟老闆確認）

**以下都是建立正式雲端資源的動作，Claude 不會主動執行，需老闆手動操作或明確同意後才進行：**

1. 開一份新的 Google 試算表（正式用），複製試算表 ID。
2. 開啟該試算表的 Extensions > Apps Script，貼上 `apps-script/Code.gs` 全部內容。
3. 修改檔頭 `CONFIG`：
   - `SPREADSHEET_ID` 填入步驟 1 的試算表 ID
   - `ADMIN_KEY` 自訂一組管理密鑰（供管理端點 API 使用，不要用本機測試的 `test-admin`）
   - `STORE_LAT` / `STORE_LNG` / `RADIUS_M` 已預填光復店座標與 20 公尺半徑，如需調整在此修改
4. 在 Apps Script 編輯器手動執行一次 `setup()` 函式（會跳出授權視窗，需同意），建立 `roster`、`events`、`leave`、`approved`、`managers` 五個分頁與表頭。
5. Deploy > New deployment > Web app：
   - Execute as：我（自己的帳號）
   - Who has access：Anyone
   - 部署後複製 Web App 網址
6. 把該網址貼到 `clock.html`**與** `manager.html` 的 `API_URL` 常數（各自取代 `PASTE_APPS_SCRIPT_URL_HERE`，兩個檔案要各改一次）。
7. 加入員工並取得每人的專屬 `key`（做法見下節「不連接排班 app 的營運方式」），組出打卡連結：
   ```
   https://你的網域或路徑/clock.html?k=員工的key
   ```
   （若 clock.html 直接放 GitHub Pages 或其他靜態網站，網址前綴依實際部署位置而定）
8. 把連結傳給對應員工即可開始使用。
9. 加入值班主管並取得專屬 `key`（`addManager('王經理')`，見下方「值班主管核定」一節），組出主管連結：
   ```
   https://你的網域或路徑/manager.html?k=主管的key
   ```
   傳給該主管即可開始核定。

（暫緩，待日後連接）原設計由排班 App 的「同步名冊」按鈕呼叫 `sync_roster` 動作批次同步在職員工、並在排班 App 的「出勤」分頁看紀錄與核准裝置；排班 app 整合暫緩期間改用下節做法，`sync_roster`／`approve_device` 等 API 動作原樣保留，日後連接直接可用。

## 不連接排班 app 的營運方式（現行做法）

排班 app 整合暫緩，名冊管理與裝置核准全部在 **Apps Script 編輯器**操作：開啟試算表 > Extensions > Apps Script，在程式碼最下方寫一個暫時函式帶參數，選它執行，結果看下方「執行紀錄」（Logger）。

### 加員工、拿專屬連結

```javascript
function run() { addEmployee('王小明'); }        // 自動編號（接續最大 E 編號，如 E03）
function run() { addEmployee('王小明', 'E10'); } // 或自訂編號
```

執行紀錄會印出：`已新增員工 王小明（E03），專屬連結參數：?k=xxxxxxxxxxxxxxxxxxxx`。把 `?k=...` 接在 clock.html 網址後面，傳給該員工。

員工離職：`function run() { deactivateEmployee('E03'); }`（active 設 false，該連結即失效，不刪資料）。

### 看打卡紀錄

直接開 Google 試算表的 `events` 分頁，一列一筆打卡事件（時間、員工、上/下班、座標、距離、是否在範圍內、裝置、狀態）。`status` 欄意義：

| status | 意義 |
|---|---|
| `ok` | 正常入帳 |
| `pending_device_approval` | 新裝置，待核准（核准後會變 `ok`） |
| `rejected_out_of_range` | 超出 20 公尺範圍，記錄但不算數 |
| `rejected_device` | 新裝置被拒，不算數 |
| `rejected_duplicate` | 重複打卡（回看 12 小時內最後一筆算數的卡是同型，上/下班未交替），記錄但不算數 |

### 員工換手機（或懷疑連結被轉傳）時的核准流程

1. 列出所有待核准裝置（姓名、裝置碼、首次時間、筆數）：
   ```javascript
   function run() { listPendingDevices(); }
   ```
2. 確認是本人換手機 → 核准（取該員工「最新一個」待核准裝置：roster 改綁新裝置，該裝置的待核准打卡全部補登為 `ok`）：
   ```javascript
   function run() { approvePendingDevice('E01'); }
   ```
3. 不是本人（連結被轉傳代打）→ 拒絕（那些打卡改 `rejected_device`，roster 不改綁）：
   ```javascript
   function run() { rejectPendingDevice('E01'); }
   ```

## 出勤月表

### 分頁機制

- 每月一個分頁，分頁名＝`yyyy-MM`（如 `2026-07`），由程式**整頁重算產生**——前 6 欄不要手動改，下次重算會整頁覆蓋（要人工修正就改 `events`／`leave` 來源資料）。**唯一可以手打的是第 7 欄「人工備註」**，見下方「人工備註欄（第 7 欄）」。
- 版面：上段「當月累計」（姓名｜參考時數｜核定時數｜異常筆數｜請假天數，依 roster 順序、只列在職員工），空一列後接明細——**一人一整月**：每位員工一個區塊（粗體姓名標題列＋該員工當月每個有紀錄日一列：日期｜星期｜班段｜參考時數｜核定時數｜狀態＋「小計」列，小計含參考／核定兩個數字），區塊間空一列。明細列到今天為止；週六日該列淺灰底、備註異常字紅色。
- **異常筆數的口徑（2026-08-19 Eason 定義）**：計入**上班忘刷卡／下班忘刷卡／少刷N組卡／遲到／早退／病假／事假**，加上沿用的**新裝置待核准**（`ABNORMAL_CATEGORIES`）。「少刷N組卡」2026-09-21 新增，本質就是忘刷卡（見下方「值班主管核定」的反向檢查），與上下班忘刷卡同等對待。
  - **比對方式是「開頭相符」不是完全相等**，因為這些字串帶變動尾巴：`遲到5分`／`早退12分`／`病假2.5h`。主管核定的狀態是多項用「、」串成一個字串（`遲到5分、早退12分`），`abnormalCategoriesOf()` 會先拆開再逐項比。
  - **計數單位是「天」不是「次」**（Eason 2026-08-19 定案）：某人某天只要命中上列任一項，**當天就算 1 筆**——不管當天發生幾次、也不管同時命中幾種。遲到又早退是 1 筆、上班忘刷卡＋下班忘刷卡是 1 筆、遲到＋忘刷卡＋病假同一天還是 1 筆。所以這一欄實質上是**「當月有幾天出現異常」**。
  - `ABNORMAL_CATEGORIES` 現在的作用是「判斷這天算不算異常」，不是計數單位；`abnormalCategoriesOf()` 回傳的清單只看**是不是空的**（清單本身保留給標紅字判斷，以及日後若要細分類別時不用重寫）。
  - ⚠ **遲到／早退不在 `c.notes` 裡**：`c.notes` 只有打卡與請假產生的註記，核定狀態是渲染明細列時才併進去顯示的，所以統計那段要另外從 `approvedMap` 取 `status_text` 併進來數（2026-08-19 實測抓到的漏算）。
  - **不計**：超出範圍嘗試（見下）、特休假／生理假／家庭照顧假／喪假／婚假（應有的假不算異常）、正常／該段無打卡／有多出的打卡段／上班中。
  - **「超出範圍嘗試」不計數但仍標紅字**（`HIGHLIGHT_CATEGORIES` ＝ `ABNORMAL_CATEGORIES` 再加這一項）：那多半是同仁人在店裡但手機定位飄掉，一天連按十幾次就把數字灌爆（A 君 8/11 一天 12 次），當異常看會失真；但仍要看得到當天被擋過。標紅字用的是**已含核定狀態**的 `statusNotes`，跟計數看同一份資料。
  - 改動只影響統計欄與紅字，**不影響打卡判定**。
- 班段＝`ok` 事件依時間配對（in 配 12 小時內的下一筆 out），跨夜班顯示 `HH:MM–HH:MM(+1)` 並歸上班那天。配不到的顯示 `？`＋備註「下班忘刷卡」／「上班忘刷卡」；**當天只要有任一筆忘刷卡，該日參考時數整天留空白**（待人工判定，核定時數不受影響，見下）。此留白規則同樣適用於最近 40 天與主管核定頁的參考欄。
- 兩個時數欄意義不同（2026-07-13 值班主管核定上線後改版）：
  - **參考時數**＝打卡時間各自取整 15 分鐘（上班往後進位／下班往前捨去）再相減加總（2026-07-15 起），2 位小數，純粹反映機器記錄，不受人工核定影響。
  - **核定時數**＝改讀 `approved` 分頁（值班主管在 `manager.html` 輸入的實際上班時段加總），**不再是** 15 分鐘取整的自動換算（`approvedHoursOfShift` 函式現在用於參考時數的 15 分取整，不再供核定欄使用）。同一 (date,emp_id) 若主管核定多次，取 `entered_at` 最新一筆；當天完全沒有核定紀錄則顯示「待核定」。核定不受打卡是否忘刷卡影響——即使當天忘刷卡，只要主管有輸入實際時段照樣顯示核定時數。
  - **狀態欄**（原「備註」）＝既有忘刷卡／新裝置待核准／超出範圍嘗試／請假別，加上主管核定帶回的比對結果：`正常`／`遲到X分`／`早退X分`／`該段無打卡`／`有多出的打卡段`／`少刷N組卡`（詳細規則見下方「值班主管核定」一節），多個並存以「、」串接。
- 自動重算：先在 Apps Script 編輯器手動執行一次 `setupMonthlyTrigger()`（重複執行不會疊加觸發器），之後每天 05:00 自動重算當月。**觸發時刻依 Apps Script 專案設定的時區，請確認為台北 (GMT+8)。**
- 凍結規則：每月 1–3 日會連上月一起重算（收尾跨夜班、補核准的裝置事件），4 日起上月分頁凍結不再變動。

### 人工備註欄（第 7 欄，2026-08-19 起）

月表明細的第 7 欄「人工備註」是**給人手打的**（例：某天忘刷卡的原因），程式不會產生內容，也不會蓋掉。

- 直接在月表該日那一列的第 7 欄打字即可。重算時的處理順序是**先收後清再放回**：`rebuildMonth()` 會先把整張月表第 7 欄的內容收進 `notes` 分頁（`日期｜姓名｜備註`）當正本，`sheet.clear()` 之後再依 (日期, 姓名) 把備註寫回對應的列。因為對回去的 key 是日期＋姓名不是列號，**名冊增減、月份天數不同都不會錯位**。
- **只認明細列**：備註要打在有日期的那一列。打在上段「當月累計」、姓名標題列或「小計」列的字不會被保存，下次重算就沒了。
- **要刪備註請改 `notes` 分頁**（刪掉那一列）。在月表上把格子清空是沒用的——收集邏輯是「只增不減」，重算時正本會再寫回來。
- `notes` 分頁也可以直接手動編輯／預先填寫，下次重算就會出現在月表上。日期格式 `yyyy-MM-dd`、姓名要與 `roster.name` 一致（比對不到的列不會出現在月表，但也不會被刪）。
- 收集失敗時**整個重算會失敗**（月表停在舊狀態）而不是照樣清空——手打的字沒有第二份，寧可重算不動也不要吞掉例外把備註洗掉。

### 10 分鐘刷新（同仁打卡後月表多快反映）

- 部署後**額外**在 Apps Script 編輯器手動執行一次 `setupMonthRefreshTrigger()`（跟 `setupMonthlyTrigger()` 是兩個獨立的觸發器，都要各跑一次；重複執行不會疊加出第二顆）。
- 之後每 10 分鐘會自動檢查 `events`、`approved`**與 `leave`** 三個分頁有沒有新增列，任一張表有新列才重算當月月表；三張表都沒新增就直接跳過，半夜幾乎零成本。也就是說：**同仁打卡後、值班主管核定後，或 Eason 手填 `leave` 分頁後，月表最多晚 10 分鐘反映**，不用再等隔天 05:00。
- 這個 10 分鐘觸發器只重算「當月」；**跨月凌晨的班（例如 8/1 凌晨 01:00 才打下班卡，收的是 7/31 的班）由既有每日 05:00 觸發器負責收尾**（05:00 觸發器在每月 1–3 日會連上月一起重算）——10 分鐘觸發器不處理這件事，不用另外設定。
- 若跟 05:00 的整月重算或手動 `rebuild_month` 剛好撞期，10 分鐘觸發器會直接讓路（搶不到鎖就跳過該次，下一次 10 分鐘後再重算），不會卡住或報錯。

### leave（請假）分頁填法

`setup()` 會建立 `leave` 分頁，Eason 手動填三欄：

| 欄 | 格式 | 範例 |
|---|---|---|
| 日期 | yyyy-mm-dd | 2026-07-09 |
| 姓名 | 須與 roster 的 name 一致（前後空白自動忽略） | 王小明 |
| 假別 | 自由填 | 特休 |

姓名比對不到 roster 的列會被**跳過**（不會報錯），填完可跑一次重算確認月表有出現。請假天數計整月（含當月還沒到的已填假單）；明細列只列到今天。

### 手動重算（rebuild_month）

改了 leave 或核准裝置後想立刻更新月表，不用等 05:00：

- 編輯器端：`function run() { rebuildMonth('2026-07'); }`（或 `dailyMonthlyRebuild()` 重算當月）。
- API 端（給日後排班 app 或指令列用）：
  ```bash
  curl -s -X POST 'APPS_SCRIPT網址' \
    -H 'Content-Type: text/plain' \
    -d '{"action":"rebuild_month","admin_key":"你的ADMIN_KEY","ym":"2026-07"}'
  # ym 可省略＝當月；回 {"ok":true,"ym":"2026-07","rows":N}
  ```
- `rebuild_month` 是 GAS 專屬 action（依賴試算表分頁），mock server 不實作。

## 定位精確度（`accuracy_m`，2026-08-19 起）

`events` 分頁最後一欄記手機回報的定位誤差（公尺，`position.coords.accuracy`）。**加在最後一欄，既有 10 欄不位移；舊資料此欄為空。** 表頭若還是舊的 10 欄，`handleClock` 會就地補寫（`readSheetAsObjects` 是照表頭取值，不補的話新欄位永遠讀不回來），不必手動跑 `setup()`。

- **為什麼要存**：判「超出範圍」時只有座標，分不出「定位很準、人真的在店外」還是「人在店裡但定位不準」。GPS 通常 5–30 m；Wi-Fi／基地台定位常見 50–150 m。2026-08-11 A 君當天 12 筆全被擋，是靠座標反推「南北固定偏 77 m、東西亂跳」才判定是 Wi-Fi 定位——有這一欄就一眼看得出來。
- **門檻** `LOW_ACCURACY_M = 50`（Code.gs 與 mock 兩處同步）。超過即視為這次定位不可信；門市半徑只有 20 m，誤差 50 m 以上的座標本來就不足以判斷人在不在店裡。
- **同仁端**：`clock` 與 `whoami.today_events` 都回 `accuracy_m` 與 `low_accuracy`。打卡被判 `rejected_out_of_range` **且** `low_accuracy` 為真時，統一失敗訊息後面加一段「這次定位可能不準（誤差約 ±X 公尺）：請關掉 Wi-Fi、或確認手機『精確位置』已開啟」；今日紀錄該筆顯示「超出範圍（誤差±Xm）」。**定位很準卻在店外時不顯示這段**——不要給不在店裡的人現成藉口。平常也不顯示誤差數字（對同仁沒意義）。
- 前後端部署順序**不互卡**：舊後端收到多的 `accuracy` 欄位會忽略；舊前端不帶此欄則存空、`low_accuracy` 為假、提示不出現。
- 本機測試：`clock.html` 的 `loc=` 旁多一個 localhost 限定的 `acc=`（如 `?loc=away&acc=120`）可模擬定位誤差。

## 定位判定：重抓與誤差折抵（2026-08-21 起）

打卡失敗實測有**兩種**成因，處理方式不同：

| 型態 | 症狀 | 例子 |
|---|---|---|
| 定位不準 | 誤差大、位置飄掉 | A 君 2026-08-11，飄到店北 77m，連按 12 次全被擋 |
| 定位過時 | 誤差小但位置是舊的 | 2026-08-20 14:45 同一分鐘先報 1201.6m（誤差±13.1）再報 6.6m |

- **前端會自動重抓**（`getPosition`）：第一次的定位若 `accuracy > GOOD_ACCURACY_M`(40) **或** 時間戳距今超過 `STALE_FIX_MS`(30 秒)，就改用 `watchPosition` 再等更好的一次，預算 `RETRY_BUDGET_MS`(8 秒)，一拿到合格的就立刻走。**第一次就合格則立刻送出，正常打卡完全不會變慢**（2026-08-19～20 實際 33 筆誤差最大 30.9m，全數低於門檻）。重抓時畫面顯示「定位中…（訊號較弱，再抓一次）」，不讓同仁以為當掉。
  - ⚠ **挑選最佳定位時，新鮮度優先於誤差大小**（`fixRank` 再比 `fixAccuracy`）。只比誤差是錯的——過時的定位常常誤差更小（8/20 那筆 ±13.1 比新鮮的 ±16.8 還「好」），只比誤差會留下錯的那筆，等於白重抓。這個 bug 在 2026-08-21 的瀏覽器實測中抓到並修掉。
  - ⚠ 前端**刻意不做「距離門市多遠」的判斷**：門市座標再複製一份到前端，日後校正要同步的地方就從三處變四處（見 2026-07-13 座標沒同步白測一輪）。改看時間戳，一樣抓得到過時定位且不需要知道門市在哪。
- **後端改判「距離扣掉誤差」**（`handleClock`）：`effectiveDistance = max(0, distance_m - min(accuracy_m, ACCURACY_CREDIT_CAP_M))`，超過 `RADIUS_M` 才判 `rejected_out_of_range`。折抵上限 100 公尺，避免誤差爆掉時變成誰都能過。**手機沒回報誤差時折抵 0，行為與加這條之前完全相同。** `distance_m` 欄位仍存原始距離，判定隨時可重算。
  - 上限取 100 的依據：A 君 8/11 第一次按（80.8m）就會過（她只需要成功一次）；8/20 那筆 1201.6m 扣掉後仍有 1188.5m，照樣擋。
  - 代價：站在店外、定位又不準的人可能過得了，最遠約落在同一個街廓內。這一關靠主管核定守。
- 本機測試：`clock.html` 的 `loc=` 多一個 `drift`（店北約 80 公尺，模擬 A 君情境），搭配 `acc=` 使用，例：`?loc=drift&acc=120`。

## 打卡頁的今日時數（自助查詢）

`clock.html` 在今日打卡清單下方會多顯示一行今日時數摘要（`whoami` 回應新增 `today_hours: {reference, approved, working_since}`），讓同仁自己就能查、不用來問 Eason：

- 有進行中的班段（今日最後一筆 `ok` 事件是尚未配對的上班卡）→「上班中（HH:mm 起）・已完成時段參考 X 小時」，`HH:mm` 為打卡的原始時間。
- 無進行中班段 → 「今日參考時數：X 小時」。
- 只計入今日**已完成**的班段（`in`＋`out` 都是 `ok` 且已配對），算法與出勤月表的參考時數同一套規則（見上方「出勤月表」章節），不重算兩套邏輯。
- **只顯示參考，不顯示核定**（2026-07-13 值班主管核定上線後改版，Eason 定案）：核定時數改由值班主管在 `manager.html` 核定實際上班時段，同仁打卡頁不再呈現「核定」字樣或數字；`whoami`/`my_recent` 後端仍原樣回傳 `approved` 欄位（給其他用途保留），只是前端不顯示。

### 回查最近 40 天（my_recent）

今日區塊下方有「查看最近 40 天」按鈕，點了才向後端要資料（action `my_recent`，payload 同 `whoami`：`{key, device_id}`），再點可收合。回傳 `days`：最近 40 天（台北時間，含今天）每天一筆 `{date, segments:[{in,out,cross}], reference, approved, notes}`，無事件的日子省略。慣例與出勤月表一致：班段歸上班（in）那一天、跨夜段顯示 `(+1)`、缺卡的一端顯示 `？` 並註記「上班忘刷卡」／「下班忘刷卡」（今天尚未下班的班段例外，註記「上班中」）；`reference`/`approved` 只計入該日已完成班段，`reference` 忘刷卡整天回 `null`（此留白規則同樣適用於出勤月表與主管核定頁的參考欄，2026-07-16 起統一）。驗證與 `whoami` 相同（key 無效即拒；裝置只回 `device_state` 提示，唯讀查詢不因裝置不符而拒回）。前端只顯示 `approved`（核定，無核定→顯示「待核定」），忘刷卡當天若主管已核定，仍顯示核定數字（不像參考時數整天留白）。

## 打卡頁的天氣動態背景（2026-08-19 新增）

`clock.html` 的背景會依**光復店當下的天氣**自動換情境：下雨就下雨、晴天就出太陽、雷雨會打閃電、入夜換月亮星星。走真實感路線：雨有遠近景深與動態模糊、玻璃上有會滑落的水珠、地面有濺起的水花，太陽是光束＋鏡頭光斑，雲是雜訊算出來的真雲層，閃電是有分岔的電光。背景火焰影片已依 Eason 指示移除（2026-08-19），紅底保留；字卡與按鈕都在效果之上，不影響閱讀與點擊。

| 天氣 | 畫面 |
|---|---|
| 晴（白天） | 右上角大太陽：光束緩慢旋轉、光暈呼吸，還有相機對著太陽拍才有的鏡頭光斑 |
| 晴（夜間） | 帶月海陰影與大氣光暈的月亮＋會閃爍的星星，畫面壓深 |
| 多雲／陰 | 碎形雜訊算出的雲層兩層不同速度飄移（多雲白天仍看得到太陽） |
| 霧 | 雲層壓低放大成貼地霧氣，整片對比降低 |
| 雨 | 雨絲帶動態模糊與遠近景深，玻璃上的水珠積重了會滑落留下水痕，底緣有濺起的水花 |
| 雷雨 | 雨勢加大、風更斜，每 5～16 秒打一道有分岔的閃電＋整片閃光 |
| 雪 | 雪花飄落並左右搖擺（台灣機率極低，但代碼有涵蓋就一起做了） |

雨勢會依天氣代碼分級：毛毛雨最稀、大雨最密最快。

### 資料來源與更新

- 天氣取自 [Open-Meteo](https://open-meteo.com/) 的 `current=weather_code,is_day`，**免金鑰、免註冊**，座標寫死光復店（與 `apps-script/Code.gs` 的 `CONFIG.STORE_LAT/STORE_LNG` 同一組）。
- 每 20 分鐘更新一次，結果存在手機的 `localStorage`（key：`mala_wx_cache`）。**開頁先用上次的天氣立刻上場，再背景更新**，所以天氣完全不會拖慢打卡畫面。
- 頁面從背景切回前景時也會順便重抓，同仁一直開著頁面也會跟著換天氣。
- **抓不到天氣（沒網路、API 掛掉、瀏覽器太舊）就完全不套用情境**，畫面維持原本的紅底背景，打卡功能不受任何影響。天氣抓取有 6 秒逾時，訊號差時不會卡住。

### 省電與無障礙

- 動畫只在頁面可見時跑，切到背景或鎖屏立即停。太陽、水珠、雲這些貴的圖層都預先烘成貼圖，每格只做貼圖，不重算漸層。
- 粒子數量依螢幕大小計算並設上限（雨滴最多 170、雪花 120、星星 110），畫布解析度最高 1.75 倍。
- 手機開了「減少動態效果」（iOS 輔助使用／Android 無障礙）時，只畫靜止的一幀，不跑動畫。
- 全部效果都是 canvas 即時算的，**沒有新增任何圖片或影片檔**；火焰影片移除後頁面還少載一支 1MB 的 mp4。

### 預覽（不用等真的下雨）

在打卡網址後面加 `&weather=` 就能指定情境，方便在群組給大家看或自己驗收：

```
clock.html?k=員工的key&weather=rain          # 下雨
clock.html?k=員工的key&weather=storm         # 雷雨（等 5～16 秒會閃電）
clock.html?k=員工的key&weather=clear         # 大太陽
clock.html?k=員工的key&weather=clear&night=1 # 月亮＋星星
```

可用值：`clear`／`cloudy`／`overcast`／`fog`／`drizzle`／`rain`／`heavy-rain`／`storm`／`snow`，可再加 `&night=1` 看夜間版。不加 `weather` 就是照真實天氣走。

### 日後要調整時

程式都在 `clock.html` 裡：色調 CSS 在 `/* ── 天氣動態背景 ── */` 註解那段，效果 JS 在檔尾最後一個 `<script>`（獨立 IIFE，與打卡邏輯完全分開，改壞了不會影響打卡）。

- 換門市座標 → 改該段開頭的 `STORE_LAT`／`STORE_LNG`
- 天氣多久重抓 → `CACHE_MS`（預設 20 分鐘）
- 雨太大／太小 → `rebuild()` 裡的密度倍率，或 `sceneOf()` 各代碼的 `power`
- 整片色調要更深或更淺 → CSS 那組 `body.wx-xxx .wxTint { background: … }`

### 授權提醒

Open-Meteo 免費 API 的條款是「非商業用途、每日 10,000 次以內」。以本店的量（每人每天開幾次打卡頁、又有 20 分鐘快取）遠低於上限，但嚴格說店務屬商業使用。若老闆日後想完全合規，兩條路都不用改前端架構：買 Open-Meteo 的商用方案（換一個 API 網域），或改接中央氣象署開放資料平臺（需申請免費授權碼；因為本 repo 是 public，授權碼建議放在 Apps Script 後端代轉，不要寫進 `clock.html`）。

## 值班主管核定（2026-07-13 新增）

同仁打卡算出的時數今後一律叫「參考時數」，是打卡時間取整 15 分鐘再相減加總（2026-07-15 起），不是實際核定依據。**核定時數改由當天值班主管在專用網頁輸入每位同仁的實際上班時段**，系統自動比對打卡與輸入時段標出遲到/早退，供 Eason 判斷薪資與績效。

### 流程

1. 值班主管當天用手機打開 `manager.html?k=主管專屬金鑰`（見下方網址格式），預設顯示今天。
2. 頁面列出當天有打卡紀錄的同仁：姓名、打卡段（沿用月表慣例，未配對顯示`？`）、參考時數。
3. 主管依實際狀況輸入每位同仁的上班時段（可多段，例如 09:00–13:30、14:00–17:30，用「＋加一段」新增、「－」移除），按「送出核定」。
4. 系統計算核定時數（＝輸入時段總長加總）與遲到/早退比對，立即顯示在頁面上，並寫入試算表 `approved` 分頁。
5. 若核定錯誤，同一天同一人可重新輸入再送出一次；系統只會**追加**新的一筆，讀取（`manager.html` 重新整理、出勤月表）一律取最新一筆為準，不會刪除舊紀錄。
6. 出勤月表的「核定時數」欄與「狀態」欄會在 10 分鐘內自動反映主管剛核定的結果（見上方「10 分鐘刷新」）。

### manager.html 網址格式

```
https://你的網域或路徑/manager.html?k=主管專屬金鑰
```

跟 `clock.html` 一樣，`?k=` 是唯一的認證方式，沒有帳號密碼。本機測試網址：`http://localhost:8899/manager.html?k=testmgr1&api=/api`（`testmgr1` 為 mock 種子主管「測試主管」）。**主管頁沒有 `?loc=` 定位模擬機制**（不需要定位，跟員工打卡頁不同）。

### addManager 用法（新增值班主管）

跟 `addEmployee` 同一套慣例，在 Apps Script 編輯器手動執行：

```javascript
function run() { addManager('王經理'); }
```

執行紀錄會印出：`已新增值班主管 王經理，主管專頁網址參數：?k=xxxxxxxxxxxxxxxxxxxx`。把 `?k=...` 接在 `manager.html` 網址後面給該主管。

主管離職／停用：`function run() { deactivateManagerByKey('該主管的key'); }`（active 設 false，不刪列）。

### 判定規則（無寬限）

- **核定時數**＝主管輸入的所有時段長度加總（跨夜段：時段 end ≤ start 視為隔天，例如 22:00–02:00 算 4 小時）。
- 比對規則：每個「輸入時段」找重疊最大的一個「打卡段」（只比對打卡 in/out 都完整的段）：
  - 該打卡段 `in` **晚於**輸入時段起點 → 標記「遲到X分」（X＝分鐘差）。
  - 該打卡段 `out` **早於**輸入時段終點 → 標記「早退X分」。
  - 早到或晚走**不算遲到/早退**、也**不加時數**——核定時數永遠等於主管輸入的時段長度，不會因為同仁早到晚走而多算。
  - 完全找不到任何重疊的打卡段 → 依成因分兩種措辭（2026-08-19 起）：當天**有送出但沒入帳的打卡**（`status!=='ok'`，例如整批被判超出範圍、或新裝置待核准，口徑同 `mgr_day` 的 `attempts`）→ 標記「**打卡未入帳，主管補登**」；當天**完全沒送出過**（純忘刷卡）→ 沿用「**該段無打卡**」。分辨的意義：前者是同仁有打、系統沒收，後者才是真的沒打。
  - 打卡的完整段數多於被用掉的輸入時段數（同仁多打了主管沒圈進去的段）→ 額外標記「有多出的打卡段」。
  - **反向：中間有間隔的兩個輸入時段配到同一條打卡段 → 標記「少刷N組卡」（2026-09-21 新增）。** 起因是許正昊 9/21：一天兩段班（11:00–14:30、17:00–21:45）只刷了第一段的上班卡與第二段的下班卡，中間兩張都沒刷。`pairShifts` 是「一張 in 配 16 小時內的下一筆 out」，這兩張在它眼裡就是完整的一長段，`unmatchedIns`／`unmatchedOuts` 都是空的 → **整天一個忘刷卡都標不出來、參考時數也不留白**（實測參考 10.25H vs 核定 8.25H，差 2 小時而表上全無紅字）。唯一看得出破綻的地方就是核定比對：兩個明明中間有間隔的時段，重疊最大的打卡段竟然是同一條。
    - ⚠ **間隔為 0 不算**（前一段終點＝下一段起點）：那是主管把一段連續班拆成兩段核定（例：分開算加班），同仁中間本來就不必刷卡，標少刷是冤枉人。
    - ⚠ 只看完整段。未配對的一端在 `pairShifts` 已經是忘刷卡了，不在這裡重複標。
    - ⚠ **遲到／早退照原樣算不動**：那兩個數字來自真實的頭尾打卡時間，仍然是對的（許正昊那天＝`遲到2分、早退1分、少刷1組卡`）。
    - **這個字樣會流進全勤的忘刷計數**（`payMissingGroups()` → `payCollect` 的 `forget_punch`／`forget_day`，出勤總表的「忘刷」欄同口徑）：**一組算 2 次**（中間漏掉的下班卡＋上班卡），與「每漏一張卡算一次」同口徑；`forget_day` 照樣同一天只算 1 天。不接這段的話，「頭尾各刷一張、中間兩張都沒刷」的人忘刷次數是 0、全勤照領，反而比老實刷四張只漏一張的人少被扣——規則會對不老實的人寬鬆。
    - ⚠ 這裡**不跳過今天**（`pairShifts` 的未配對 in 要跳過今天，因為那是「上班中」）：少刷是「完整長段吃掉兩個核定時段」才成立，人早就下班了，沒有那種歧義。
  - 都沒有異常 → 狀態顯示「正常」。
  - 多個標記以「、」串接（如「遲到10分、早退10分」）。
- 這套比對邏輯在 `apps-script/Code.gs`（`computeApprovalStatus`）與 `mock/mock_server.py`（`compute_approval_status`）各實作一份，保持同步；純函式可直接用 node 載入 `Code.gs` 測試，不需要試算表。
- **狀態是核定當下的一次性快照，靠 `recheckPendingApprovalStatuses()` 回頭修**（每日 05:00 由 `dailyMonthlyRebuild` 呼叫，也可用管理 API `recheck_approvals` 手動跑）：掃近 `RECHECK_STATUS_DAYS` 天內 `status_text` 含「該段無打卡」（或「打卡未入帳」「少刷N組卡」）的核定紀錄，用**當下最新**的打卡資料重算；狀態有變才追加一筆修正紀錄（`periods`／`approved_hours` 原封不動只換 `status_text`，`manager_name` 加註「（系統重算）」）。
  - **`RECHECK_STATUS_DAYS` 2026-08-19 由 3 天放寬為 40 天**（Eason 指定）。原本 3 天只涵蓋「週五核定、週六才打完卡」，同仁隔久一點補打卡就永遠修不到。成本與天數幾乎無關——讀三張表是一次性的，迴圈只在「那天真的有『該段無打卡』紀錄」時才進到重算。
  - 回頭修的對象**三種措辭都算**（`該段無打卡`／`打卡未入帳`／`少刷N組卡`）——待核准裝置事後被核准會讓當天的卡翻成 `ok`，那天就真的比對得到了，要能修得到；「少刷N組卡」同理，中間那組卡事後入帳會把長段拆開，少刷就該自動消失。
  - ⚠ **它只換 `status_text`**，`periods`／`approved_hours` 一律原封不動，核定時數不會因為重算而改變。
  - ⚠ 打卡從頭到尾沒進系統的日子（整批被擋、或忘刷卡沒補），重算後狀態仍然對不到段——但措辭會依上面的規則落在「打卡未入帳，主管補登」或「該段無打卡」，那是事實描述、不是 bug。

### 套補丁到四家店（deploy-to-clasp.py，2026-09-21）

repo 的 `apps-script/Code.gs` 的 `CONFIG` 是 `PASTE_SPREADSHEET_ID_HERE`／`PASTE_ADMIN_KEY_HERE` 佔位符，**整份複製到 clasp 專案會把四家店的正式試算表 ID、ADMIN_KEY、店座標、`PAYROLL_API` 全部洗掉，打卡會整個掛**。所以每次改後端都得逐份手動套補丁——這支把那件事變成可重跑的動作：

```bash
cd ~/mala-clock-in && git fetch origin main        # 基準版要新，否則整批誤判成漂移
cd ~/mala-gas/mala-clock-in && clasp pull          # 四個專案都先 pull
python3 ~/mala-clock-in/tools/deploy-to-clasp.py           # dry-run，看會改什麼
python3 ~/mala-clock-in/tools/deploy-to-clasp.py --apply   # 真的套進去
python3 ~/mala-clock-in/tools/verify-store-backends.py     # 四家都要 ✓
# 確認無誤後，各專案自己 clasp push
```

- 做法：拿 repo 的新版 `Code.gs`，把**該店現有檔案裡的 `CONFIG` 區塊原封不動接回去**。`Payroll.gs` 沒有店別專屬的值（無 `PASTE_`／`SPREADSHEET_ID`／`ADMIN_KEY`），整份覆蓋；套到哪個專案是用 `grep PAYROLL_HANDLERS` 找出來的，不是寫死的。
- **預設 dry-run**，要 `--apply` 才寫檔；**不碰 clasp**（不 pull 也不 push——`clasp push` 照守則要先問過 Eason 再自己按）。
- **漂移防護**：寫之前先確認「該店現有檔案 ＝ 基準版（預設 `origin/main`）＋ 該店 CONFIG」。對不起來就停下來印 diff、不覆蓋，除非 `--force`。這道是 2026-08-23「補丁被誤判成已套用 → 出差保護靜默失效」的解藥。
- 寫完檢查成品不含 `PASTE_`，含就中止。
- 「對不起來，沒動」是正常會遇到的：看過 diff 確定那些差異可以丟掉再 `--force`；該保留的先補進 repo 再重跑。**不要無腦 `--force`。**

### 一次性回填「少刷N組卡」（backfill_missing_groups，2026-09-21）

反向檢查只在**主管按下核定的當下**跑，所以新規則只對之後的核定生效。已經核定完的日子不會自己變（許正昊 9/21 就停在「遲到2分、早退1分」），上面那支 `recheckPendingApprovalStatuses` 的前置篩選只看三種措辭，也篩不到它們。要把歷史補回來就跑這支：

```bash
# ① 先 dry-run，看它想改什麼（不帶 apply，一列都不會寫）
curl -s -X POST 'APPS_SCRIPT網址' \
  -H 'Content-Type: application/json' \
  -d '{"action":"backfill_missing_groups","admin_key":"你的ADMIN_KEY","from":"2026-09-01","to":"2026-09-30"}'

# ② 確認 hits 沒問題，再帶 apply:true 跑第二次才真的寫入
curl -s -X POST 'APPS_SCRIPT網址' \
  -H 'Content-Type: application/json' \
  -d '{"action":"backfill_missing_groups","admin_key":"你的ADMIN_KEY","from":"2026-09-01","to":"2026-09-30","apply":true}'
```

- 回傳 `{scanned, fixed, hits:[{date,emp_id,name,from,to,periods,hours}], skipped:[{...,reason}], applied}`。
- **預設 dry-run**：`apply` 不是 `true` 就只回報、不寫入。
- **刻意不掛進每日 05:00 觸發器**：那等於在發薪前無聲地整批改寫狀態。這支只給人工跑。
- 三道保守閘（`backfillMissingPunchGroups`）：
  1. 只掃「核定時段 ≥2 段」且「狀態還沒有少刷字樣」的紀錄，其餘直接跳過。
  2. 重算後**只接受「差別僅是多了少刷N組卡」**（`statusDiffIsOnlyMissingGroup`）；遲到分鐘數也跟著變的那種列進 `skipped` 不寫，不趁機改到別的。
  3. 主管手動認定的遲到（狀態帶 `(認定)` 或「主管認定不計遲到」）一律不動，列進 `skipped`。
- 寫入方式與 `recheckPendingApprovalStatuses` 相同：append-only、`periods`／`approved_hours` 原封不動只換 `status_text`、`manager_name` 加「（系統重算）」，所以看得出是系統補的，原紀錄也留著。
- 區間防呆：日期須 `yyyy-MM-dd`、`to >= from`、跨度上限 `BACKFILL_MAX_DAYS`（366 天），違反回 `bad_date`／`bad_range`／`range_too_wide`。
- ⚠ 補完狀態之後，**全勤的忘刷次數要等薪資那邊重新歸集才會跟著變**（`payCollect` 是每次結算時讀 `approved` 現況算的，不是快照），所以順序是「先回填、再結算」。

### 試算表分頁

- `approved`：`date, emp_id, name, periods, approved_hours, status_text, manager_name, entered_at`。只追加不覆蓋；同一 (date,emp_id) 若有多筆，一律以 `entered_at` 最新的為準（讀取端處理，分頁本身不刪舊列）。`periods` 欄格式為 `"09:00-13:30,14:00-17:30"`。
- `managers`：`name, key, active`。`setup()` 會自動建立空分頁，主管靠 `addManager()` 加入。

### API（GAS 與 mock 皆實作）

- `mgr_day {mgr_key, date?}`（date 缺省＝今天，Asia/Taipei）→ 驗證主管金鑰後回傳 roster 全部在職同仁（當天有打卡的在前、依第一筆事件時間排序；沒打卡的殿後也可核定）：`{emp_id, name, segments, reference, approved?, attempts}`，`approved` 只在已有核定紀錄時出現（`periods, approved_hours, status_text, manager_name`）；`attempts`＝該日未入帳的打卡嘗試筆數（被拒／待核准都算），前端在無入帳班段時顯示「今天沒有入帳的打卡（有 N 筆未入帳嘗試）」。`reference` 忘刷卡整天回 `null`（此留白規則同樣適用於最近 40 天與出勤月表的參考欄，不是沒打卡才 `null`）。
- `mgr_approve {mgr_key, date, emp_id, periods:[{start:'HH:mm', end:'HH:mm'}]}` → 驗證格式（`HH:mm` 需為合法時間、至少 1 段）後計算核定時數與狀態，append 到 `approved`，回傳計算結果。

### 月表欄位新定義（2026-07-13 起）

出勤月表每人每日呈現三欄（詳見上方「出勤月表」章節）：**參考時數**（打卡時間取整 15 分鐘相減加總，2026-07-15 起，不受核定影響）｜**核定時數**（讀 `approved` 分頁最新一筆，無紀錄顯示「待核定」）｜**狀態**（既有忘刷卡/新裝置/超出範圍/請假註記，加上主管核定帶回的正常/遲到/早退等）。`approvedHoursOfShift` 函式現在用於參考時數（月表／`whoami`/`my_recent` 皆同一套 15 分取整算法），不再供「核定」欄使用——核定欄改讀主管手動輸入的 `approved` 分頁。

## 店內公告（2026-08-28 新增）

值班主管在核定頁最下方的「店內公告」發布，同仁一打開打卡頁，最上方就會看到。

- **呈現方式**：一次顯示一整則的完整內容；只有一則時完全不動，多則才每 5 秒輪流換一則（右上角小圓點指示第幾則）。
  刻意不做橫向跑馬燈——同仁打完卡就關頁，跑馬燈一輪要 20 秒以上，重點常常還沒捲出來人就走了。
- **沒有公告時整塊不出現**，打卡頁與加這個功能之前完全一樣。
- **誰能發**：凡持有主管金鑰的人（同「新進同仁報到」與「同仁異動」的權限範圍），發布者記在 `created_by`。
- **下架日**（選填）：填了就顯示到那天為止（含當天），留空＝一直顯示到手動下架。
- **下架不是刪除**：`active` 改成 FALSE，紀錄留在 `notices` 分頁，可以再按「重新顯示」。
- **上限**：單則 `NOTICE_MAX_LEN`（120）字；同仁端最多顯示 `NOTICE_MAX_SHOW`（5）則，新的優先。
- **三家店各發各的**：公告存在各店自己的試算表，互不干擾。

### 資料與 API

`notices` 分頁：`id / text / active / ends_on / created_at / created_by`（分頁不存在時一律當作沒有公告，
不報錯；第一次發布會自動建立分頁，不必特地跑 `setup()`）。

| action | 誰用 | 說明 |
|---|---|---|
| `whoami` 的 `notices` 欄位 | 同仁打卡頁 | 啟用中且未過下架日的公告，新的排前面，最多 5 則 |
| `mgr_notices {mgr_key}` | 主管頁 | 全部公告（含已下架、已過期），供管理 |
| `mgr_add_notice {mgr_key, text, ends_on?}` | 主管頁 | 發布 |
| `mgr_set_notice_active {mgr_key, id, active}` | 主管頁 | 下架／重新顯示 |

⚠ 公告是主管手打的自由文字，**兩端都必須當純文字處理**：同仁端用 `textContent`、
主管端清單先過 `esc()` 轉義。改這一段時不要為了排版方便換成 `innerHTML`。

## 注意事項

- 階段一不做 PIN 密碼、不做登入，認人靠「專屬連結 key ＋ GPS ＋ 裝置綁定」三者組合，這是設計文件的刻意決定。
- `events` 分頁只會逐筆新增，絕不覆蓋既有紀錄。
- `sync_roster` 對既有員工只更新 `name`/`active`，絕不覆蓋 `key`/`device_id`/`device_bound_at`。
- 開發/測試階段請勿對正式 Google 試算表做會覆蓋既有資料的操作。
